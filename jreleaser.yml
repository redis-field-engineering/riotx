environment:
  properties:
    jdkPathPrefix: 'plugins/riotx/build/jdks'
    jdkFilePrefix: 'zulu17.32.13-ca-jdk17.0.2'

project:
  name: riotx
  description: Get data in and out of Redis Enterprise with RIOTX
  longDescription: |
    RIOTX is a RIOT extension designed to help enterprises get data in and out of Redis.
  links:
    homepage: https://github.com/redis-field-engineering/riotx
    documentation: https://redis.com/riotx
  authors:
    - Julien Ruaux
  license: Apache-2.0
  inceptionYear: '2024'
  stereotype: CLI
  vendor: Redis
  icons:
    - url: https://raw.githubusercontent.com/redis-field-engineering/riotx/main/src/media/icon_256x256.png
      width: 256
      height: 256
  java:
    groupId: com.redis
    version: '21'
    multiProject: true
    mainClass: com.redis.riotx.Main
  tags: ['riotx', 'redis', 'enterprise', 'cloud', 'input', 'output', 'tool', 'data', 'import', 'export']

platform:
  replacements:
    aarch_64: aarch64

release:
  github:
    overwrite: true
    sign: true
    issues:
      enabled: true
    changelog:
      formatted: ALWAYS
      preset: conventional-commits
      contributors:
        format: '- {{contributorName}}{{#contributorUsernameAsLink}} ({{.}}){{/contributorUsernameAsLink}}'
      contentTemplate: 'src/jreleaser/changelog.tpl'
      extraProperties:
        categorizeScopes: 'true'
      labelers:
        - label: 'i18n'
          title: 'regex:^(?:i18n(?:\(.*\))?!?):\s.*'
          order: 120
        - label: 'dependencies'
          title: 'regex:^(?:deps(?:\(.*\))?!?):\s.*'
          order: 130
        - label: 'allcontributors1'
          title: 'regex:^docs: update .all-contributorsrc.*'
          order: -1
        - label: 'allcontributors2'
          title: 'regex:^docs: update CONTRIBUTORS.md.*'
          order: -2
      categories:
        - title: '🌐 Translation'
          key: 'i18n'
          order: 70
          labels:
            - 'i18n'
        - title: '⚙️  Dependencies'
          key: 'dependencies'
          order: 80
          labels:
            - 'dependencies'
        - title: 'allcontributors'
          order: 59
          labels:
            - 'allcontributors1'
            - 'allcontributors2'
      hide:
        categories:
          - 'merge'
          - 'allcontributors'
        contributors:
          - 'GitHub'
          - 'allcontributors'
          - 'dependabot'
      replacers:
        - search: 'i18n: '
        - search: 'deps: '

signing:
  active: ALWAYS
  armored: true

deploy:
  active: ALWAYS
  maven:
    active: ALWAYS
    github:
      packages:
        active: ALWAYS
        url: https://maven.pkg.github.com/redis

announce:
  slack:
    active: RELEASE
    channel: '#field-engineering-announcements'
    message: '🧨 RIOTX {{projectVersion}} has been released! {{releaseNotesUrl}}'

assemble:
  jlink:
    riotx-standalone:
      active: ALWAYS
      java:
        version: '21'
      imageName: '{{distributionName}}-{{projectEffectiveVersion}}'
      executable: riotx
      fileSets:
        - input: '.'
          includes:
            - LICENSE
      jdeps:
        multiRelease: base
        ignoreMissingDeps: true
        targets:
          - 'plugins/riotx/build/libs/riotx-{{projectVersion}}.jar'
      additionalModuleNames:
        - 'java.security.sasl'
        - 'java.security.jgss'
        - 'jdk.crypto.cryptoki'
        - 'jdk.crypto.ec'
        - 'jdk.localedata'
        - 'jdk.net'
        - 'jdk.security.auth'
        - 'jdk.security.jgss'
      targetJdks:
        - path: '{{jdkPathPrefix}}/zulu17Osx/{{jdkFilePrefix}}-macosx_x64/zulu-17.jdk/Contents/Home'
          platform: 'osx-x86_64'
        - path: '{{jdkPathPrefix}}/zulu17OsxArm/{{jdkFilePrefix}}-macosx_aarch64/zulu-17.jdk/Contents/Home'
          platform: 'osx-aarch_64'
        - path: '{{jdkPathPrefix}}/zulu17Linux/{{jdkFilePrefix}}-linux_x64'
          platform: 'linux-x86_64'
        - path: '{{jdkPathPrefix}}/zulu17LinuxArm/{{jdkFilePrefix}}-linux_aarch64'
          platform: 'linux-aarch_64'
        - path: '{{jdkPathPrefix}}/zulu17LinuxMusl/{{jdkFilePrefix}}-linux_musl_x64'
          platform: 'linux_musl-x86_64'
        - path: '{{jdkPathPrefix}}/zulu17LinuxMuslArm/{{jdkFilePrefix}}-linux_musl_aarch64'
          platform: 'linux_musl-aarch_64'
        - path: '{{jdkPathPrefix}}/zulu17Windows/{{jdkFilePrefix}}-win_x64'
          platform: 'windows-x86_64'
        - path: '{{jdkPathPrefix}}/zulu17WindowsArm/{{jdkFilePrefix}}-win_aarch64'
          platform: 'windows-aarch_64'
      mainJar:
        path: 'plugins/riotx/build/libs/riotx-{{projectVersion}}.jar'
      jars:
        - pattern: 'plugins/riotx/build/dependencies/flat/*.jar'

files:
  artifacts:
    - path: VERSION
      extraProperties:
        skipChecksum: 'true'
        skipSigning: 'true'
        skipSbom: 'true'
